# Smart Hospital Queue System (GUI Edition)

เป้าหมายคือการสร้างโปรแกรมจัดการคิวโรงพยาบาลที่มีหน้าต่างกราฟิก (GUI) โดยใช้ Java (Swing) ซึ่งมีฟีเจอร์หลักดังนี้:
1. เก็บบันทึกข้อมูลประวัติผู้ป่วยลงในไฟล์ (File I/O) ทำให้สามารถดึงข้อมูลเก่ามาใช้งานได้
2. มีการประเมินอาการ (Triage) เพื่อแบ่งแผนกและให้คะแนนความฉุกเฉิน
3. ใช้โครงสร้างข้อมูล (Data Structures) ที่สร้างขึ้นมาเอง:
   - **Priority Queue**: ใช้สำหรับแผนก **EMERGENCY** เท่านั้น (เรียงตามความเร่งด่วน)
   - **Normal Queue (FIFO)**: ใช้สำหรับแผนกอื่นๆ (CARDIO, NEURO, ORTHO, GENERAL) (มาก่อนได้ก่อน)
4. มีหน้าต่าง (JFrame) สำหรับการโต้ตอบกับผู้ใช้งานตาม Flow ที่กำหนด

## User Review Required

1. **GUI Framework**: แนะนำให้ใช้ **Java Swing** เพราะเป็นไลบรารีพื้นฐานที่ติดมากับ Java ไม่ต้องติดตั้งเพิ่ม เหมาะสำหรับโปรเจคระดับมหาวิทยาลัยที่ทำครั้งแรก
2. **รูปแบบการเก็บไฟล์**: แนะนำให้เก็บเป็นไฟล์ Text ธรรมดา (เช่น `patients.csv` หรือ `.txt`) เพื่อให้คุณสามารถเปิดไฟล์เข้าไปดูและทำความเข้าใจได้ง่ายๆ ว่าโปรแกรมเซฟข้อมูลลงไปอย่างไร

## Open Questions

ไม่มีคำถามเพิ่มเติมครับ หากแผนนี้โอเค เราจะค่อยๆ เริ่มทำไปทีละ Phase ครับ

## Proposed Changes (แผนการเรียนรู้และลงมือทำทีละขั้นตอน)

ผมแบ่งโปรเจคใหญ่นี้ออกเป็น **4 Phase** เพื่อให้เราค่อยๆ ทำไปทีละส่วนและให้คุณเข้าใจหลักการทำงานได้เต็มที่:

### Phase 1: ข้อมูลผู้ป่วย & ระบบไฟล์ (Patient Data & File I/O)
* **สิ่งที่จะทำ**: สร้างคลาส `Patient` เพื่อเก็บข้อมูล (ID, ชื่อ, อายุ, เพศ, โรคประจำตัว, กรุ๊ปเลือด) 
* **สิ่งที่จะทำ**: สร้างคลาส `FileManager` ที่มี Method ในการเซฟข้อมูลผู้ป่วยใหม่ลงไฟล์ และค้นหาผู้ป่วยเก่าจาก ID (อ่านไฟล์)
* **สิ่งที่คุณจะได้เรียนรู้**: OOP เบื้องต้น, การอ่านเขียนไฟล์ (FileReader / FileWriter) ใน Java

### Phase 2: โครงสร้างข้อมูลคิว (Custom Data Structures)
* **สิ่งที่จะทำ**: สร้างคลาส `Node` สำหรับ Linked List
* **สิ่งที่จะทำ**: สร้างคลาส `MyQueue` (แบบมาก่อนได้ก่อน) สำหรับแผนกทั่วไป
* **สิ่งที่จะทำ**: สร้างคลาส `MyPriorityQueue` สำหรับแผนกฉุกเฉิน (จะเรียงลำดับคนที่อาการหนักสุดขึ้นมาก่อน)
* **สิ่งที่คุณจะได้เรียนรู้**: การทำ Linked List, การทำ Queue และการเขียน Algorithm จัดเรียง (Sorting/Insertion) ง่ายๆ

### Phase 3: ระบบคัดกรองและแบ่งแผนก (Triage & Hospital Manager)
* **สิ่งที่จะทำ**: สร้างคลาส `TriageSystem` สำหรับคำนวณคะแนน Severity จากอาการที่ป้อนเข้ามา
* **สิ่งที่จะทำ**: สร้างคลาส `HospitalManager` เพื่อเป็นตัวกลางคอยรับผู้ป่วย และแยกเข้า `MyQueue` หรือ `MyPriorityQueue` ประจำ 5 แผนกให้ถูกต้อง
* **สิ่งที่คุณจะได้เรียนรู้**: การประยุกต์ใช้ Rule-based logic, การจัดการ Object หลายๆ ตัว

### Phase 4: สร้างหน้าต่างโปรแกรม (GUI - Java Swing)
* **สิ่งที่จะทำ**: สร้างหน้าจอ 3 หน้าหลัก ได้แก่
  1. **Login/Register Frame**: หน้ากรอก ID ถ้ารู้จักให้ผ่าน ถ้าไม่รู้จักให้กรอกประวัติเพิ่ม
  2. **Triage Frame**: หน้าแสดงประวัติและรับข้อมูลอาการป่วย
  3. **Dashboard Frame**: หน้าจอรวมคิวทั้งหมดของโรงพยาบาล แบ่งตามแผนก
* **สิ่งที่คุณจะได้เรียนรู้**: การสร้างหน้าต่าง (JFrame), การรับค่าจากช่องกรอก (JTextField), การผูกปุ่ม (JButton) เข้ากับการทำงาน (Action Listener)

---

## Verification Plan

### การทดสอบ (เราจะทดสอบกันไปทีละ Phase)
1. **Phase 1**: ลองรันโค้ดสั้นๆ สร้างผู้ป่วย เซฟลงไฟล์ แล้วลองค้นหาจาก ID ว่าข้อมูลกลับมาครบไหม
2. **Phase 2**: ลองสร้างคิวสมมติ แล้วดึงข้อมูลออกทีละคน ว่าเรียงลำดับถูกต้องตามกฎ (คิวปกติ vs คิวฉุกเฉิน) หรือไม่
3. **Phase 3**: ลองจำลองการส่งข้อมูลอาการเข้าไป ว่าระบบคำนวณคะแนนและแยกแผนกได้ถูกต้องไหม
4. **Phase 4**: ลองรันหน้าต่าง GUI และกดคลิกใช้งานเสมือนผู้ป่วยมาโรงพยาบาลจริงๆ
