# สรุปโครงสร้างและฟีเจอร์ของโปรเจกต์ (Smart Hospital Queue System)

เอกสารนี้สรุปภาพรวมทั้งหมดของโปรเจกต์ ที่เราได้พัฒนาร่วมกันจนเสร็จสมบูรณ์ เพื่อให้คุณใช้เป็นแนวทางในการนำเสนอ (Present) โครงงานได้ง่ายขึ้นครับ

---

## 🏗️ โครงสร้างข้อมูล (Data Structures) ที่ใช้ในโปรเจกต์
โปรเจกต์นี้ไม่ได้ใช้ Collection Framework สำเร็จรูปของ Java (เช่น `java.util.Queue` หรือ `java.util.Stack`) แต่เราเขียน Data Structure ขึ้นมาเองทั้งหมดจากศูนย์ เพื่อแสดงความเข้าใจในรายวิชา:

1. **`Node` (Linked List)**
   - เป็นรากฐานของโครงสร้างข้อมูลทั้งหมด เก็บ Object `Patient` และตัวชี้ไปยัง Node ถัดไป (`next`)
2. **`MyQueue` (FIFO - First In First Out)**
   - ใช้กับแผนกทั่วไป (`GENERAL`, `CARDIO`, `ORTHO`, `NEURO`) ใครมาก่อนได้คิวก่อน (ต่อท้ายคิว)
3. **`MyPriorityQueue` (Priority-based Insertion)**
   - ใช้กับแผนก `EMERGENCY` โค้ดจะทำการจัดเรียง (Sort) ตอนเพิ่มข้อมูล (Enqueue) โดยเช็คจาก `priorityScore` ใครคะแนนสูงกว่าจะถูกแทรกคิวไปอยู่ด้านหน้าคนที่คะแนนน้อยกว่า
4. **`MyStack` (LIFO - Last In First Out)**
   - ใช้สำหรับระบบ **ประวัติการรักษา (History)** เมื่อคนไข้ถูกดึงออกจากคิว (Dequeue) จะถูกนำมาใส่ (Push) ลงใน Stack ทำให้คนไข้ที่เพิ่งถูกเรียกล่าสุดอยู่บนสุดเสมอ

---

## ⚙️ ระบบและฟีเจอร์หลัก (Core Features)

### 1. ระบบจัดการผู้ป่วย (Patient Management)
- คลาส `Patient` ทำหน้าที่เก็บข้อมูลส่วนตัว (ชื่อ, อายุ, โรคประจำตัว) และสถานะคิว (คะแนนความฉุกเฉิน, แผนกที่ถูกส่งไป, อาการเบื้องต้น)
- **Persistence (กันข้อมูลหาย)**: ข้อมูลประวัติผู้ป่วยทั้งหมดจะถูกเซฟลงไฟล์ `patients_db.csv` ทำให้การปิด-เปิดโปรแกรมใหม่ ข้อมูลก็ยังอยู่ครบ

### 2. ระบบคัดกรองอาการ (Triage System)
- คำนวณคะแนน `priorityScore` จากระดับความเจ็บปวด (Pain), การหายใจ (Breathing), อุณหภูมิ และกลุ่มเสี่ยง (เด็ก/ผู้สูงอายุ)
- **Routing Logic**: วิเคราะห์คำสำคัญ (Keyword) จากอาการ เช่น เจอคำว่า "หน้าอก" จะส่งเข้าแผนก `CARDIO` หรือถ้าเป็นเคส "วิกฤต" จะส่งเข้าแผนก `EMERGENCY` ทันทีพร้อมคะแนนทะลุหลอด (1000 คะแนน)

### 3. ระบบเซฟสถานะคิวและประวัติแบบรายวัน (Daily State Persistence)
- **ไฟล์ `queue_YYYY-MM-DD.csv`**: ระบบจะเซฟสถานะของคิวทั้ง 5 แผนก (รวมถึง แผนก และ อาการ ของแต่ละคน) ทุกครั้งที่มีการขยับคิว ป้องกันไฟดับคิวหาย
- **ไฟล์ `history_YYYY-MM-DD.csv`**: ระบบจะเซฟประวัติการเรียกคิวของแต่ละวันไว้ สามารถเรียกดูย้อนหลังได้ตลอดเวลา

---

## 🖥️ หน้าต่างการใช้งาน (GUI Navigation Flow)

โปรแกรมประกอบด้วย 4 หน้าต่างหลักที่เชื่อมโยงกันอย่างสมบูรณ์:

1. **`LoginFrame` (หน้าแรก)**
   - ค้นหาผู้ป่วยจาก ID ถ้าเจอจะเด้งข้อความโชว์ประวัติพร้อมโรคประจำตัว
   - ถ้าไม่เจอ จะเปิดป๊อปอัปให้ลงทะเบียนผู้ป่วยใหม่
   - *ฟีเจอร์เสริม*: มีปุ่ม **"ข้ามไปหน้าจัดการคิว (Dashboard)"** สำหรับเจ้าหน้าที่ที่ต้องการดูคิวเลยโดยไม่ต้องเพิ่มผู้ป่วย

2. **`TriageFrame` (หน้าซักประวัติ)**
   - รับข้อมูลอาการ, ระดับความเจ็บปวด, อุณหภูมิ เพื่อส่งให้ `TriageSystem` ประเมินผล และจัดคนไข้ลงคิวที่ถูกต้อง จากนั้นจะสลับไปหน้า Dashboard อัตโนมัติ

3. **`DashboardFrame` (หน้าจอหลักของพยาบาล)**
   - แสดงคิวแยกเป็น 5 แผนกชัดเจน (EMERGENCY จะโชว์คะแนนกำกับ ส่วนแผนกอื่นจะเรียงตามลำดับปกติ)
   - มีปุ่ม **"เรียกคิวถัดไป"** ประจำแต่ละแผนก เพื่อนำคนไข้ออกจากคิว และส่งเข้า Stack ประวัติ
   - มีปุ่ม **"รับผู้ป่วยใหม่"** (วนกลับไป LoginFrame)
   - มีปุ่ม **"ดูประวัติการรักษา"** เพื่อเปิดหน้าต่าง `HistoryFrame` (ใช้ Stack ในการแสดงผลเรียงจากคนล่าสุดที่ถูกเรียก)

4. **`HistoryFrame` (หน้าต่างดูประวัติ)**
   - ค้นหาประวัติการถูกเรียกคิวตามวันที่ได้
   - แสดงข้อมูล ชื่อ, แผนกที่เข้า, และอาการ (เรียงลำดับคนที่ถูกเรียกหลังสุดอยู่บนสุด)

---

## 🚀 การอัพเดทโค้ดล่าสุด
- แก้ไขบัคแผนกหายเมื่อเปิดโปรแกรมใหม่ โดยเพิ่มการดึงและเซฟตัวแปร `department` กลับเข้าตัวผู้ป่วย
- เพิ่มการเซฟและแสดงผล `symptoms` (อาการ) ในหน้าประวัติการรักษา
- ปรับแต่งการแสดงเลขลำดับในหน้าประวัติให้ **นับจากล่างขึ้นบน** (คนที่โดนเรียกคนแรกอยู่ล่างสุดและได้เลข 1)
- เพิ่มปุ่มข้ามไปหน้า Dashboard ทันทีที่หน้า Login
